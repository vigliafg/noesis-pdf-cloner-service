# ── Servizio (venv .venv) ──────────────────────────────────────────────────
# Web API + frontend
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9

# Modelli e validazione
pydantic>=2.7

# PDF: split, merge, render miniature, lettura /PageLabels
pymupdf>=1.24

# CLI: barra di avanzamento
tqdm>=4.66
